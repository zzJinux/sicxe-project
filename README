##컴파일환경 및 실행환경 
  Linux, MacOS

##컴파일
  프로젝트의 루트에서 `make` 실행
  `make`는 `make debug`와 같은 컴파일을 수행하며
  `make debug`는 결과물을 디버깅할 때 쓴다.
  `make release`는 디버깅 옵션을 제거하고 컴파일을 수행한다.

##컴파일 리셋
  프로젝트 루트에서 `make clean` 실행

##실행 방법
  프로젝트 루트에서 `./20131604.out` 실행